/**
 * @param {number[]} nums
 * @param {number} val
 * @return {number}
 */
const removeElement = function (nums, val) {
  let i = 0;
  for (let j = 1; j < nums.length; j += 1) {
    if (nums[i] === val && nums[j] !== val) {
    }
  }
};
